# Runtime dependencies for feature_extraction.py
# Deliberately minimal: the original needed only pandas/numpy/joblib, and rich
# is optional (the script degrades to plain log-line progress without it).
pandas>=2.0
numpy>=1.24
joblib>=1.3
rich>=13.0        # optional: live progress bar with ETA
